enum UserRole {
    customer
    provider
    admin
}

enum UserStatus {
    active
    suspend
}

enum DietaryType {
    veg
    non_veg
    vegan
}

enum PaymentMethod {
    cod
    online
}

enum OrderStatus {
    placed
    preparing
    ready
    delivered
    cancelled
}